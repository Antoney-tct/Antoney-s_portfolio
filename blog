<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Same head -->
    <style>
        /* === BLOG ENHANCEMENTS === */
        .blog-grid {
            display: grid;
            gap: 2.5rem;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            padding: 6rem 2rem;
        }

        .blog-card {
            background: linear-gradient(145deg, #0a0a1a 0%, #00101a 100%);
            border: 1px solid var(--primary);
            border-radius: 16px;
            padding: 2rem;
            position: relative;
            overflow: hidden;
            transition: var(--transition);
        }

        .blog-card::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, 
                transparent 45%, 
                rgba(0, 240, 255, 0.1) 50%, 
                transparent 55%);
            animation: blogGlow 6s infinite linear;
        }

        @keyframes blogGlow {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .blog-card:hover {
            transform: translateY(-10px);
            box-shadow: 0 10px 30px rgba(0, 240, 255, 0.2);
        }

        .post-meta {
            color: var(--gray);
            font-size: 0.9rem;
            margin: 1.5rem 0;
            display: flex;
            gap: 1.5rem;
        }

        .read-more {
            position: relative;
            color: var(--primary);
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            padding: 0.8rem 1.5rem;
            border: 1px solid var(--primary);
            border-radius: 50px;
            overflow: hidden;
            transition: var(--transition);
        }

        .read-more::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, 
                transparent, 
                rgba(0, 240, 255, 0.2), 
                transparent);
            transition: left 0.6s;
        }

        .read-more:hover::before {
            left: 100%;
        }
    </style>
</head>
<body>
    <!-- Header -->

    <section class="diagonal-section">
        <div class="section-title">
            <h2 class="cyber-glow">Tech Chronicles</h2>
            <p class="subtitle">Insights from the Digital Frontier</p>
        </div>
        
        <div class="blog-grid">
            <!-- Enhanced Blog Card -->
            <article class="blog-card neon-card">
                <div class="card-inner">
                    <div class="post-header">
                        <h3 class="cyber-glow">MySQL Optimization Techniques</h3>
                        <div class="post-meta">
                            <span><i class="fas fa-microchip"></i> Database</span>
                            <span><i class="fas fa-clock"></i> 8 min read</span>
                        </div>
                    </div>
                    <p class="excerpt">Discover how strategic indexing reduced query times by 40% in our Student Management System...</p>
                    <a href="#" class="read-more">
                        Decrypt Article <i class="fas fa-terminal"></i>
                    </a>
                </div>
            </article>

            <!-- Add more cards -->
        </div>
    </section>
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Same head -->
    <style>
        /* === HOLOGRAPHIC EFFECTS === */
        .case-study-header {
            text-align: center;
            padding: 8rem 2rem 6rem;
            background: radial-gradient(circle at 50% 20%, #00101a 0%, #0a0a0e 100%);
            position: relative;
        }

        .hologram-effect {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: 
                linear-gradient(45deg, 
                    transparent 65%, 
                    rgba(0, 149, 255, 0.1) 75%, 
                    transparent 85%),
                linear-gradient(-45deg, 
                    transparent 65%, 
                    rgba(255, 0, 170, 0.1) 75%, 
                    transparent 85%);
            animation: hologramScan 12s infinite linear;
        }

        @keyframes hologramScan {
            0% { background-position: 0 0, 0 0; }
            100% { background-position: 100% 100%, 100% 100%; }
        }

        .tech-stack {
            display: flex;
            gap: 1.5rem;
            flex-wrap: wrap;
            justify-content: center;
            margin: 3rem 0;
        }

        .tech-chip {
            background: rgba(0, 240, 255, 0.1);
            padding: 0.8rem 1.5rem;
            border-radius: 30px;
            border: 1px solid var(--primary);
            backdrop-filter: blur(5px);
            transition: var(--transition);
        }

        .tech-chip:hover {
            background: rgba(0, 240, 255, 0.2);
            transform: scale(1.05);
        }

        .case-study-content {
            max-width: 800px;
            margin: 0 auto;
            padding: 2rem;
            background: rgba(10, 10, 14, 0.6);
            border-radius: 20px;
            border: 1px solid rgba(0, 240, 255, 0.2);
        }

        .code-snippet {
            background: #00101a;
            padding: 1.5rem;
            border-radius: 12px;
            border-left: 4px solid var(--primary);
            font-family: 'Courier New', monospace;
            margin: 2.5rem 0;
            position: relative;
            overflow: hidden;
        }

        .code-snippet::before {
            content: 'SQL';
            position: absolute;
            top: 0;
            right: 0;
            padding: 0.3rem 1rem;
            background: var(--primary);
            color: var(--dark);
            font-size: 0.8rem;
            border-radius: 0 0 0 8px;
        }
    </style>
</head>
<body>
    <!-- Header -->

    <section class="case-study-header">
        <div class="hologram-effect"></div>
        <h1 class="cyber-glow" style="font-size: 3.5rem;">Student Management System</h1>
        <div class="tech-stack">
            <span class="tech-chip"><i class="fas fa-database"></i> MySQL</span>
            <span class="tech-chip"><i class="fas fa-project-diagram"></i> Normalization</span>
            <span class="tech-chip"><i class="fas fa-shield-alt"></i> Data Integrity</span>
        </div>
    </section>

    <!-- Rest of content -->
</body>
</html>

<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Same head -->
    <style>
        /* === NEON SERVICE TIERS === */
        .service-tier {
            display: grid;
            gap: 4rem;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            padding: 6rem 2rem;
        }

        .pricing-card {
            background: linear-gradient(145deg, #0a0a1a 0%, #00101a 100%);
            padding: 2.5rem;
            border: 2px solid var(--secondary);
            border-radius: 20px;
            position: relative;
            overflow: hidden;
            transition: var(--transition);
        }

        .pricing-card::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, 
                transparent 45%, 
                rgba(0, 149, 255, 0.1) 50%, 
                transparent 55%);
            animation: serviceGlow 8s infinite linear;
        }

        .price-tag {
            font-size: 2.8rem;
            background: linear-gradient(45deg, var(--primary), var(--accent));
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            margin: 1.5rem 0;
            font-weight: 700;
            text-shadow: 0 0 15px rgba(0, 240, 255, 0.3);
        }

        .service-features li {
            padding: 1rem 0;
            border-bottom: 1px solid rgba(0, 240, 255, 0.1);
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .service-features li::before {
            content: '✓';
            color: var(--accent);
            font-weight: 700;
        }
    </style>
</head>
<body>
    <!-- Header -->

    <section class="diagonal-section">
        <div class="section-title">
            <h2 class="cyber-glow">Digital Services</h2>
            <p class="subtitle">Custom solutions for the cyber age</p>
        </div>

        <div class="service-tier">
            <!-- Web Dev Card -->
            <div class="pricing-card neon-card">
                <div class="card-inner">
                    <i class="fas fa-code fa-3x" style="color: var(--primary);"></i>
                    <h3>Full-Stack Development</h3>
                    <div class="price-tag">$75/hr</div>
                    <ul class="service-features">
                        <li>React/Node.js Applications</li>
                        <li>REST API Development</li>
                        <li>Cloud Integration</li>
                        <li>CI/CD Pipelines</li>
                    </ul>
                    <a href="#contact" class="cta-button">
                        Initiate Project <i class="fas fa-rocket"></i>
                    </a>
                </div>
            </div>
        </div>
    </section>
</body>
</html>

